{
  "mcpServers": {
    "project-docs": {
      "command": "npx",
      "args": [
        "-y",
        "REPLACE_WITH_A_REVIEWED_AND_PINNED_MCP_PACKAGE"
      ],
      "env": {
        "API_TOKEN": "${env:PROJECT_DOCS_API_TOKEN}"
      }
    }
  }
}
