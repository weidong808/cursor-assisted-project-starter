# Ship

Prepare the current change for merge or release.

1. Read `AGENTS.md` and confirm the diff matches the stated scope.
2. Run `bash scripts/validate-starter.sh` when this repository's structure changed.
3. Run the project's format, lint, type, test, and build commands that actually exist.
4. Summarize user-visible behavior, validation evidence, rollback notes, and remaining risk.
5. Stop before any deploy, publish, or external write unless I explicitly authorize it.

Do not claim a check passed unless it was actually run.
